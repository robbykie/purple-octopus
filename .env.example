# Quote provider: "yahoo" (default, live) or "demo" (offline fixtures, no network)
QUOTES_PROVIDER=yahoo

# Seconds a quote is served from the in-memory cache before refetching upstream.
QUOTES_CACHE_TTL=30
